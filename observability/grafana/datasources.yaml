apiVersion: 1
datasources:
  - name: Tempo
    type: tempo
    access: proxy
    # Grafana (in the observability network) reaches Tempo by service name.
    url: http://tempo:3200
    isDefault: true
    jsonData:
      httpMethod: GET
